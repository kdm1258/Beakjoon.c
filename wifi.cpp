#include <bits/c++.h>
using namespace std;
using ll = long long
int n,c;
vector<int> house;
int distance(int key){
	
}
int main(void){
	cin>>n>>c;
	for (i = 0 ; i < n ; i++){
		int tmp;
		cin>>tmp;
		int max_x = 0;
		max(max_x, tmp);
		house.push_back(tmp);
	}
	int l = 0;
	int r = max_x;
	while(l<=r){
		int mid = (l + r) / 2;
		if (
	}
}
